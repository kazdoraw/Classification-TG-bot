# Руководство по обучению моделей

## Этап 3: Обучение первичного классификатора

### Обзор

Созданы три скрипта обучения для сравнения различных архитектур:

1. **`train_primary_baseline_cnn.py`** - Простой CNN (обучение с нуля)
2. **`train_primary_resnet18.py`** - ResNet-18 (transfer learning) ⭐ ОСНОВНАЯ
3. **`train_primary_vit.py`** - Vision Transformer (transfer learning)

### Особенности реализации

#### Общие характеристики всех скриптов

- ✅ **Работа с дисбалансом классов** через class weights в loss function
- ✅ **Аугментации данных** для обучающей выборки
- ✅ **Early stopping** для предотвращения переобучения
- ✅ **Learning rate scheduling** (ReduceLROnPlateau / CosineAnnealing)
- ✅ **Метрики**: Accuracy, F1-score, Confusion Matrix
- ✅ **Автосохранение лучшей модели** по val_accuracy
- ✅ **Визуализация** training history и confusion matrix
- ✅ **Classification report** с детализацией по классам

#### 1. Baseline CNN

**Архитектура:**
```
4 сверточных блока (Conv + BatchNorm + ReLU + MaxPool)
32 -> 64 -> 128 -> 256 каналов
2 полносвязных слоя (512 -> 3)
Dropout (0.5)
```

**Параметры обучения:**
- Batch size: 32
- Epochs: 100 (с early stopping)
- Learning rate: 0.001
- Optimizer: Adam
- Обучение с нуля (no pretrained weights)

**Запуск:**
```bash
python src/train_primary_baseline_cnn.py
```

**Ожидаемое время:** ~30-60 минут на CPU

#### 2. ResNet-18 ⭐ ОСНОВНАЯ МОДЕЛЬ

**Архитектура:**
- Предобученная ResNet-18 (ImageNet weights)
- Замена финального FC слоя (512 -> 3)
- Transfer learning

**Параметры обучения:**
- Batch size: 16
- Epochs: 50 (с early stopping)
- Learning rate: 0.001
- Optimizer: Adam
- Weight decay: 1e-4

**Запуск:**
```bash
python src/train_primary_resnet18.py
```

**Ожидаемое время:** ~20-40 минут на CPU

**Преимущества:**
- Лучший баланс качество/скорость
- Меньше параметров чем ViT
- Быстрый инференс
- Рекомендуется для продакшна

#### 3. Vision Transformer (ViT-B/16)

**Архитектура:**
- Предобученный ViT-B/16 (ImageNet weights)
- Замена classification head (768 -> 3)
- Transfer learning

**Параметры обучения:**
- Batch size: 8 (меньше из-за размера модели)
- Epochs: 30 (меньше для экономии времени)
- Learning rate: 0.0001 (ниже для fine-tuning)
- Optimizer: AdamW
- Scheduler: CosineAnnealingLR
- Gradient clipping (max_norm=1.0)

**Запуск:**
```bash
python src/train_primary_vit.py
```

**Ожидаемое время:** ~60-120 минут на CPU

**Примечание:** ViT требует больше вычислительных ресурсов.

### Сравнение моделей

После обучения всех трех моделей запустите скрипт сравнения:

```bash
python src/compare_primary_models.py
```

**Что делает скрипт:**
- Загружает все обученные модели
- Оценивает на validation set
- Выводит сравнительную таблицу метрик
- Строит графики сравнения
- Выводит classification reports
- Рекомендует лучшую модель

**Выходные файлы:**
- `models/model_comparison.png` - графики сравнения

### Структура выходных файлов

Каждая модель сохраняется в отдельной папке:

```
models/
├── baseline_cnn/
│   ├── best_model.pth
│   ├── confusion_matrix.png
│   └── training_history.png
├── resnet18/
│   ├── best_model.pth
│   ├── confusion_matrix.png
│   └── training_history.png
├── vit/
│   ├── best_model.pth
│   ├── confusion_matrix.png
│   └── training_history.png
└── model_comparison.png
```

### Формат сохраненной модели

Каждый checkpoint содержит:
```python
{
    'epoch': int,                    # Номер эпохи
    'model_state_dict': state_dict,  # Веса модели
    'optimizer_state_dict': state_dict,  # Состояние оптимизатора
    'val_acc': float,                # Validation accuracy
    'val_f1': float,                 # Validation F1-score
    'class_names': list,             # ['retina', 'blood', 'scene']
    'config': dict,                  # Конфигурация обучения
}
```

### Рекомендации по запуску

#### Последовательность обучения

1. **Начните с ResNet-18** (основная модель):
   ```bash
   python src/train_primary_resnet18.py
   ```

2. **Затем Baseline CNN** (для сравнения):
   ```bash
   python src/train_primary_baseline_cnn.py
   ```

3. **Опционально ViT** (если есть время):
   ```bash
   python src/train_primary_vit.py
   ```

4. **Сравните результаты**:
   ```bash
   python src/compare_primary_models.py
   ```

#### Ускорение обучения

Если обучение слишком долгое:

1. **Уменьшите количество эпох** в CONFIG внутри скрипта
2. **Заморозьте ранние слои** в ResNet/ViT:
   ```python
   # Раскомментируйте в create_model():
   for param in model.parameters():
       param.requires_grad = False
   ```
3. **Используйте меньший batch size** (но может ухудшить качество)

### Ожидаемые результаты

#### Типичные метрики (ориентировочно)

| Модель | Accuracy | F1-Score | Inference Speed |
|--------|----------|----------|-----------------|
| Baseline CNN | 0.75-0.85 | 0.73-0.83 | Быстро |
| ResNet-18 | 0.85-0.95 | 0.84-0.94 | Быстро |
| ViT-B/16 | 0.87-0.96 | 0.86-0.95 | Медленно |

**Примечание:** Результаты зависят от:
- Качества данных
- Случайной инициализации
- Параметров аугментаций
- Дисбаланса классов (retina: 40 vs blood/scene: 200)

### Проблемы и решения

#### Проблема 1: Out of Memory (OOM)

**Симптомы:** RuntimeError: CUDA out of memory или система зависает

**Решения:**
- Уменьшите batch_size в CONFIG
- Закройте другие приложения
- Используйте CPU вместо GPU (уже по умолчанию)

#### Проблема 2: Низкое качество на классе retina

**Причина:** Дисбаланс классов (40 vs 200 изображений)

**Решения:**
- ✅ Уже используются class weights
- Добавьте больше аугментаций для retina
- Соберите больше данных retina

#### Проблема 3: Переобучение (overfitting)

**Симптомы:** Train accuracy >> Val accuracy

**Решения:**
- ✅ Early stopping уже включен
- Увеличьте weight_decay
- Добавьте больше аугментаций
- Уменьшите learning rate

#### Проблема 4: Медленное обучение

**Решения:**
- Уменьшите num_epochs
- Увеличьте batch_size (если позволяет память)
- Заморозьте ранние слои в transfer learning моделях
- Используйте только ResNet-18 (пропустите ViT)

### Следующие шаги

После успешного обучения и выбора лучшей модели:

1. **Скопируйте лучшую модель** для использования в боте:
   ```bash
   cp models/resnet18/best_model.pth models/primary_resnet18.pth
   ```

2. **Переходите к Этапу 4**: Обучение вспомогательных моделей
   - U-Net для сегментации
   - YOLO для детекции
   - CIFAR-10 классификатор

3. **Документируйте результаты** для отчета:
   - Скриншоты confusion matrix
   - Training history графики
   - Таблица сравнения моделей

---

## Дополнительная информация

### Мониторинг обучения

Все скрипты выводят подробную информацию в консоль:
- Loss и метрики по эпохам
- Лучшая модель и когда сохранена
- Classification report в конце обучения

### Визуализация результатов

После обучения проверьте:
- `confusion_matrix.png` - где модель ошибается
- `training_history.png` - динамика обучения
- Логи в консоли - финальные метрики

### Использование обученной модели

Пример загрузки для инференса:
```python
import torch
from torchvision import models
import torch.nn as nn

# Загрузка модели
checkpoint = torch.load('models/resnet18/best_model.pth')
model = models.resnet18(weights=None)
model.fc = nn.Linear(model.fc.in_features, 3)
model.load_state_dict(checkpoint['model_state_dict'])
model.eval()

# Инференс
with torch.no_grad():
    output = model(image_tensor)
    pred = output.argmax(dim=1)
```

---

**Удачи в обучении моделей!** 🚀
